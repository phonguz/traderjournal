# --- Created by Slick DDL
# To stop Slick DDL generation, remove this comment and start using Evolutions

# --- !Ups

create table "COMMENTS" ("id" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"TEXT" VARCHAR NOT NULL,"AUTHOR" BIGINT NOT NULL,"DATE" TIMESTAMP NOT NULL);
create table "USERS" ("id" BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,"EMAIL" VARCHAR NOT NULL,"FIRST_NAME" VARCHAR NOT NULL,"LAST_NAME" VARCHAR NOT NULL);
alter table "COMMENTS" add constraint "COMMENTS_AUTHOR_FK" foreign key("AUTHOR") references "USERS"("id") on update NO ACTION on delete NO ACTION;

# --- !Downs

alter table "COMMENTS" drop constraint "COMMENTS_AUTHOR_FK";
drop table "COMMENTS";
drop table "USERS";

